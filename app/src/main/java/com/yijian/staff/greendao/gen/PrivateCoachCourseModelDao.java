package com.yijian.staff.greendao.gen;

import android.database.Cursor;
import android.database.sqlite.SQLiteStatement;

import org.greenrobot.greendao.AbstractDao;
import org.greenrobot.greendao.Property;
import org.greenrobot.greendao.internal.DaoConfig;
import org.greenrobot.greendao.database.Database;
import org.greenrobot.greendao.database.DatabaseStatement;

import com.yijian.staff.db.bean.PrivateCoachCourseModel;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "PRIVATE_COACH_COURSE_MODEL".
*/
public class PrivateCoachCourseModelDao extends AbstractDao<PrivateCoachCourseModel, Long> {

    public static final String TABLENAME = "PRIVATE_COACH_COURSE_MODEL";

    /**
     * Properties of entity PrivateCoachCourseModel.<br/>
     * Can be used for QueryBuilder and for referencing column names.
     */
    public static class Properties {
        public final static Property Idx = new Property(0, Long.class, "idx", true, "_id");
        public final static Property Id = new Property(1, String.class, "id", false, "ID");
        public final static Property ConsumingMinute = new Property(2, Integer.class, "consumingMinute", false, "CONSUMING_MINUTE");
        public final static Property MemberCourseId = new Property(3, String.class, "memberCourseId", false, "MEMBER_COURSE_ID");
        public final static Property MemberCourseName = new Property(4, String.class, "memberCourseName", false, "MEMBER_COURSE_NAME");
    }


    public PrivateCoachCourseModelDao(DaoConfig config) {
        super(config);
    }
    
    public PrivateCoachCourseModelDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(Database db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"PRIVATE_COACH_COURSE_MODEL\" (" + //
                "\"_id\" INTEGER PRIMARY KEY AUTOINCREMENT ," + // 0: idx
                "\"ID\" TEXT," + // 1: id
                "\"CONSUMING_MINUTE\" INTEGER," + // 2: consumingMinute
                "\"MEMBER_COURSE_ID\" TEXT," + // 3: memberCourseId
                "\"MEMBER_COURSE_NAME\" TEXT);"); // 4: memberCourseName
    }

    /** Drops the underlying database table. */
    public static void dropTable(Database db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"PRIVATE_COACH_COURSE_MODEL\"";
        db.execSQL(sql);
    }

    @Override
    protected final void bindValues(DatabaseStatement stmt, PrivateCoachCourseModel entity) {
        stmt.clearBindings();
 
        Long idx = entity.getIdx();
        if (idx != null) {
            stmt.bindLong(1, idx);
        }
 
        String id = entity.getId();
        if (id != null) {
            stmt.bindString(2, id);
        }
 
        Integer consumingMinute = entity.getConsumingMinute();
        if (consumingMinute != null) {
            stmt.bindLong(3, consumingMinute);
        }
 
        String memberCourseId = entity.getMemberCourseId();
        if (memberCourseId != null) {
            stmt.bindString(4, memberCourseId);
        }
 
        String memberCourseName = entity.getMemberCourseName();
        if (memberCourseName != null) {
            stmt.bindString(5, memberCourseName);
        }
    }

    @Override
    protected final void bindValues(SQLiteStatement stmt, PrivateCoachCourseModel entity) {
        stmt.clearBindings();
 
        Long idx = entity.getIdx();
        if (idx != null) {
            stmt.bindLong(1, idx);
        }
 
        String id = entity.getId();
        if (id != null) {
            stmt.bindString(2, id);
        }
 
        Integer consumingMinute = entity.getConsumingMinute();
        if (consumingMinute != null) {
            stmt.bindLong(3, consumingMinute);
        }
 
        String memberCourseId = entity.getMemberCourseId();
        if (memberCourseId != null) {
            stmt.bindString(4, memberCourseId);
        }
 
        String memberCourseName = entity.getMemberCourseName();
        if (memberCourseName != null) {
            stmt.bindString(5, memberCourseName);
        }
    }

    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    @Override
    public PrivateCoachCourseModel readEntity(Cursor cursor, int offset) {
        PrivateCoachCourseModel entity = new PrivateCoachCourseModel( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // idx
            cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1), // id
            cursor.isNull(offset + 2) ? null : cursor.getInt(offset + 2), // consumingMinute
            cursor.isNull(offset + 3) ? null : cursor.getString(offset + 3), // memberCourseId
            cursor.isNull(offset + 4) ? null : cursor.getString(offset + 4) // memberCourseName
        );
        return entity;
    }
     
    @Override
    public void readEntity(Cursor cursor, PrivateCoachCourseModel entity, int offset) {
        entity.setIdx(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setId(cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1));
        entity.setConsumingMinute(cursor.isNull(offset + 2) ? null : cursor.getInt(offset + 2));
        entity.setMemberCourseId(cursor.isNull(offset + 3) ? null : cursor.getString(offset + 3));
        entity.setMemberCourseName(cursor.isNull(offset + 4) ? null : cursor.getString(offset + 4));
     }
    
    @Override
    protected final Long updateKeyAfterInsert(PrivateCoachCourseModel entity, long rowId) {
        entity.setIdx(rowId);
        return rowId;
    }
    
    @Override
    public Long getKey(PrivateCoachCourseModel entity) {
        if(entity != null) {
            return entity.getIdx();
        } else {
            return null;
        }
    }

    @Override
    public boolean hasKey(PrivateCoachCourseModel entity) {
        return entity.getIdx() != null;
    }

    @Override
    protected final boolean isEntityUpdateable() {
        return true;
    }
    
}
